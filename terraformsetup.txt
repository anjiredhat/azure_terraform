1) Install terraform.

source: https://developer.hashicorp.com/terraform/downloads?product_intent=terraform
1) wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform

2) install az cli.
source: https://learn.microsoft.com/en-us/cli/azure/install-azure-cli-linux?pivots=apt
link: curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

3) install git cli
 source: https://github.com/git-guides/install-git
 link: sudo apt-get install git-all


4) To install docker
   curl -sSL https://get.docker.com/ | sh


